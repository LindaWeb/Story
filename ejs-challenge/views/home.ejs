<%- include partials/header.ejs %>

<h1>Home</h1>

<p>
    <%= homeStartingContent %> 
</p>


<% posts.forEach(function(post) {  %>
      <h1><%= post.blogTitle %></h1>
      <p><%= post.blogBody.substring(0, 100) + " ..." %>
        <a href="posts/<%= post.blogTitle.toLowerCase() %>" class="btn btn-primary" role="button">Read More</a>
      </p>
    <hr/>
 <% }) %>


<%- include partials/footer.ejs %>