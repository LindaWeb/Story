<%- include('partials/header'); -%>

<h1>Home</h1>

<p>
    <%= homeContent %>
</p>

<% posts.forEach(function(post) { %>
    <!-- console.log(post.title)); %> -->
    <h1><%= post.title %></h1>
    <p><%= post.body.substring(0, 100) + " ..." %>  
        <a href="/posts/<%= post.title.toLowerCase() %>" class="btn btn-primary" role="button">Read more</a>
    </p>
    <hr>
<% }) %>


<%- include('partials/footer'); -%>